language: ruby


install: true

script:
  - sleep 60
  - true

notifications:
  irc:
    channels:
      - "chat.freenode.net#travisstaging"
    on_success: always
    on_failure: always
    template: "%{repository}#%{build_number} (%{duration}) (%{branch} - %{commit} : %{author}): %{message}"
